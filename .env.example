# Copy to .env.local for local dev, and set the same key in Vercel
# (Project Settings -> Environment Variables) for production.
# No trailing slash.
VITE_API_URL=https://skin-cancer-backend.onrender.com
