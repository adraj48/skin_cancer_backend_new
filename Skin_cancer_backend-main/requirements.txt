flask
flask-cors
gunicorn

# TFLite interpreter only (~5MB), not the full TensorFlow runtime (~400MB).
# TF plus an inference pass exceeded Render's 512MB free tier and the container
# was OOM-killed on every request. model.tflite was converted from
# best_model.h5 and verified to match it to 3.3e-07.
tflite-runtime==2.14.0

# tflite-runtime 2.14 is built against the numpy 1.x ABI.
numpy<2

# headless: no GUI/X11 libs. Pinned because 5.x requires numpy>=2, which the
# line above rules out.
opencv-python-headless==4.10.0.84
